# smbr2 0.0.0.9001

- Added within-chain threading support: passing `threads_per_chain` to `analyse()` automatically recompiles the model with threading enabled.
- Added a `NEWS.md` file to track changes to the package.
